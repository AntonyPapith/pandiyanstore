<!DOCTYPE html>
<html lang="en"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="theme-color" content="#FDCCE6"><title>Search: {{ $query }} | Pandiyan Store</title><link rel="icon" type="image/png" href="{{ asset('logo.png') }}"><link rel="stylesheet" href="{{ asset('css/seyon.css') }}"><link rel="stylesheet" href="{{ asset('css/product-fixes.css') }}"></head>
<body class="product-page"><main class="product-shell">
<header class="product-header search-header"><a class="product-back" href="{{ route('home') }}">&larr;</a><form class="results-search" action="{{ route('search') }}" method="GET"><input type="search" name="q" value="{{ $query }}" aria-label="Search products" required><button type="submit">Search</button></form><a class="product-cart-link" href="{{ route('cart.index') }}" aria-label="Open cart"><svg viewBox="0 0 24 24" fill="none"><path d="M3 4h2l2.2 10.2a2 2 0 0 0 2 1.6h7.7a2 2 0 0 0 1.9-1.4L21 7H6" stroke="currentColor" stroke-width="1.8"/><circle cx="10" cy="20" r="1.3" fill="currentColor"/><circle cx="18" cy="20" r="1.3" fill="currentColor"/></svg><span>{{ count(array_filter(session('cart', []), fn ($quantity) => $quantity > 0)) }}</span></a></header>
<div class="search-summary"><p>Search results for</p><h1>“{{ $query }}”</h1><span>{{ $products->total() }} product(s) found</span></div>
<div class="product-grid">@forelse($products as $product)<article class="product-card"><div class="product-card-media"><img src="{{ Storage::url($product->image_path) }}" alt="{{ $product->name }}" loading="lazy">@if($product->discount_price !== null)<span class="discount-badge">Sale</span>@endif</div><div class="product-card-body"><small class="search-category">{{ $product->category?->name }}</small><h2>{{ $product->name }}</h2><div class="product-card-price"><strong>₹{{ number_format((float)($product->discount_price ?? $product->price),2) }}</strong>@if($product->discount_price !== null)<del>₹{{ number_format((float)$product->price,2) }}</del>@endif</div><p class="product-card-stock {{ $product->quantity < 1 ? 'out' : '' }}">{{ $product->quantity > 0 ? $product->quantity.' in stock' : 'Out of stock' }}</p>@if($product->description)<p class="product-card-description">{{ $product->description }}</p>@endif<form class="add-cart-form" method="POST" action="{{ route('cart.add',$product) }}">@csrf<input type="hidden" name="quantity" value="1"><button type="submit" {{ $product->quantity < 1 ? 'disabled' : '' }}>{{ $product->quantity > 0 ? 'Add to cart' : 'Out of stock' }}</button></form></div></article>@empty<div class="product-grid-empty"><p>No products found for “{{ $query }}”.</p><a href="{{ route('home') }}">Browse categories</a></div>@endforelse</div>
<div class="search-pagination">{{ $products->links() }}</div></main><script src="{{ asset('js/store-actions.js') }}?v={{ filemtime(public_path('js/store-actions.js')) }}"></script></body></html>
