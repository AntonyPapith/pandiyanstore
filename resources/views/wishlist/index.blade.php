<!DOCTYPE html><html lang="en"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="theme-color" content="#FDCCE6"><title>Wishlist | Pandiyan Store</title><link rel="icon" href="{{ asset('logo.png') }}"><link rel="stylesheet" href="{{ asset('css/seyon.css') }}"><link rel="stylesheet" href="{{ asset('css/product-fixes.css') }}"></head><body class="product-page"><main class="product-shell">
<header class="product-header"><a class="product-back" href="{{ url()->previous() }}">&larr;</a><div><p>Saved products</p><h1>My Wishlist</h1></div><a class="product-cart-link" href="{{ route('cart.index') }}" aria-label="Open cart"><svg viewBox="0 0 24 24" fill="none"><path d="M3 4h2l2.2 10.2a2 2 0 0 0 2 1.6h7.7a2 2 0 0 0 1.9-1.4L21 7H6" stroke="currentColor" stroke-width="1.8"/><circle cx="10" cy="20" r="1.3" fill="currentColor"/><circle cx="18" cy="20" r="1.3" fill="currentColor"/></svg><span>{{ count(array_filter(session('cart', []), fn ($quantity) => $quantity > 0)) }}</span></a></header>
@if(session('wishlist_success'))<p class="cart-alert">{{ session('wishlist_success') }}</p>@endif
<div class="product-grid">@forelse($products as $product)<article class="product-card"><div class="product-card-media"><img src="{{ Storage::url($product->image_path) }}" alt="{{ $product->name }}"><form class="wishlist-toggle" method="POST" action="{{ route('wishlist.toggle', $product) }}">@csrf<button class="active" type="submit" aria-label="Remove {{ $product->name }} from wishlist">♥</button></form></div><div class="product-card-body"><h2>{{ $product->name }}</h2><div class="product-card-price"><strong>₹{{ number_format((float)($product->discount_price ?? $product->price),2) }}</strong>@if($product->discount_price !== null)<del>₹{{ number_format((float)$product->price,2) }}</del>@endif</div>@if($product->color || $product->size)<div class="cart-variant-details">@if($product->color)<span>Color: {{ $product->color }}</span>@endif @if($product->size)<span>Size: {{ $product->size }}</span>@endif</div>@endif<form class="add-cart-form" method="POST" action="{{ route('cart.add',$product) }}">@csrf<input type="hidden" name="quantity" value="1"><button type="submit" @disabled($product->quantity < 1)>{{ $product->quantity > 0 ? 'Add to cart' : 'Out of stock' }}</button></form></div></article>@empty<div class="product-grid-empty"><p>Your wishlist is empty.</p><a href="{{ route('home') }}">Browse products</a></div>@endforelse</div>
</main><script src="{{ asset('js/product-shine.js') }}"></script><script src="{{ asset('js/store-actions.js') }}?v={{ filemtime(public_path('js/store-actions.js')) }}"></script></body></html>
